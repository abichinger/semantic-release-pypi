const {publish, publishPackage} = require('../lib/publish')

const setupPy = './python_package'
const distDir = 'dist'

test('test publishPackage', async() => {
    //await expect(publishPackage(setupPy, distDir)).resolves.toBe(undefined)
})
